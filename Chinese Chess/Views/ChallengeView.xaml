<UserControl x:Class="Chinese_Chess.Views.ChallengeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             FontFamily="Georgia" Foreground="{x:Null}">

    <UserControl.Resources>
        <Style x:Key="StartGameButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="10" Color="Black" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="-2"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DifficultyRadioStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextGrayBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="DifficultyGroup"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="OuterBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock x:Name="HeaderTxt" 
                                               Text="{TemplateBinding Content}" 
                                               FontSize="20" 
                                               FontWeight="Bold" 
                                               Foreground="{TemplateBinding Foreground}"/>

                                    <TextBlock Text="{TemplateBinding Tag}" 
                                               FontSize="14"
                                               Foreground="{TemplateBinding Foreground}" 
                                               Opacity="0.7"
                                               Margin="0,5,0,0"/>
                                </StackPanel>

                                <Border x:Name="CheckSquare"
                                        Grid.Column="2"
                                        Width="24" Height="24"
                                        BorderBrush="{DynamicResource PrimaryBrush}"
                                        BorderThickness="1"
                                        CornerRadius="12"
                                        Background="Transparent"
                                        VerticalAlignment="Center">
                                    <TextBlock x:Name="CheckMark"
                                               Text="✓"
                                               FontSize="16" FontWeight="Bold"
                                               Foreground="White"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Visibility="Collapsed"/>
                                </Border>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>

                                <Setter TargetName="HeaderTxt" Property="Foreground" Value="{DynamicResource TextDarkBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextDarkBrush}"/>

                                <Setter TargetName="CheckSquare" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Background="{DynamicResource BackgroundBrush}">

        <Button Content="Back to Menu"
                FontSize="16" FontWeight="SemiBold"
                Foreground="{DynamicResource TextGrayBrush}"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30" Cursor="Hand" Panel.ZIndex="2"
                Click="BackToMenuButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="Transparent"
                            BorderBrush="{DynamicResource TextGrayBrush}"
                            BorderThickness="2"
                            CornerRadius="20"
                            Padding="20,10">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="1200" Panel.ZIndex="1" Width="800">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,50">
                <TextBlock Text="Select Difficulty"
                           FontSize="60" 
                           FontWeight="Black" 
                           Foreground="{DynamicResource TextDarkBrush}" 
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,5">
                    <TextBlock.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="5" Color="Black" Opacity="0.2"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="Choose your opponent level to begin the battle."
                           FontSize="20" 
                           Foreground="{DynamicResource TextDarkBrush}" 
                           Opacity="0.8"
                           HorizontalAlignment="Center"
                           Typography.Capitals="AllSmallCaps" />
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource CardBackgroundBrush}" 
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="30"
                        VerticalAlignment="Center" 
                        Margin="0,0,20,0">
                    <StackPanel x:Name="DifficultyOptions">
                        <RadioButton x:Name="OptionEasy" Content="Easy" Tag="Perfect for beginners." IsChecked="True" Style="{StaticResource DifficultyRadioStyle}"/>
                        <RadioButton x:Name="OptionMedium" Content="Medium" Tag="A balanced challenge." Style="{StaticResource DifficultyRadioStyle}"/>
                        <RadioButton x:Name="OptionHard" Content="Hard" Tag="For experienced players." Style="{StaticResource DifficultyRadioStyle}"/>

                        <Button Content="START GAME" Style="{StaticResource StartGameButtonStyle}" Margin="0,30,0,0" Click="StartGameButton_Click"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="120" Height="120" CornerRadius="60" 
                            Background="{DynamicResource CardBackgroundBrush}" 
                            BorderBrush="{DynamicResource BorderBrush}" 
                            BorderThickness="2" 
                            Margin="0,0,0,20">

                        <TextBlock Text="✨" 
                                   FontSize="80" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   FontFamily="Segoe UI Emoji"
                                   Foreground="{DynamicResource PrimaryBrush}"/>
                    </Border>

                    <TextBlock Text="Ready for a match?" 
                               FontSize="24" FontWeight="Bold" 
                               Foreground="{DynamicResource TextDarkBrush}" 
                               TextAlignment="Center" Margin="0,0,0,10"/>

                    <TextBlock Text="Your next move is critical. Select an option and prove your mastery of Chinese Chess."
                               Foreground="{DynamicResource TextGrayBrush}" 
                               FontSize="16"
                               TextAlignment="Center" 
                               TextWrapping="Wrap"/>
                </StackPanel>

            </Grid>
        </Grid>

        <TextBlock Text="Credits" 
                   Foreground="{DynamicResource PrimaryBrush}" 
                   HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   TextDecorations="Underline" Cursor="Hand" Margin="0,0,20,20"/>
    </Grid>
</UserControl>